<Control
    xmlns="https://spacestation14.io"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:cc="clr-namespace:Content.Client.Administration.UI.CustomControls"
    xmlns:at="clr-namespace:Content.Client.Administration.UI.Tabs.AtmosTab"
    Margin="4"
    MinSize="50 50">
    <GridContainer Columns="4">
        <cc:UICommandButton Text="{Loc atmos-tab-add-atmos}" Command="addatmos" WindowType="{x:Type at:AddAtmosWindow}" />
        <cc:UICommandButton Text="{Loc atmos-tab-add-gas}" Command="addgas" WindowType="{x:Type at:AddGasWindow}" />
        <cc:UICommandButton Text="{Loc atmos-tab-fill-gas}" Command="fillgas" WindowType="{x:Type at:FillGasWindow}" />
        <cc:UICommandButton Text="{Loc atmos-tab-set-temperature}" Command="settemp"
                             WindowType="{x:Type at:SetTemperatureWindow}" />
    </GridContainer>
</Control>
